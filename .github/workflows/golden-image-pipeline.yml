name: Golden Image Factory Pipeline

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      os_choice:
        description: 'Target Operating System'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - ubuntu
          - rhel
          - windows
      scan_only:
        description: 'Skip Packer Build (Run OpenSCAP Scan Only)'
        required: true
        default: false
        type: boolean
      azure_region:
        description: 'Deployment Region (Leave blank to use default, southcentralus)'
        required: false
        type: string
      ubuntu_image_name_override:
        description: 'Custom Ubuntu Image Name Override (Optional)'
        required: false
        type: string
      rhel_image_name_override:
        description: 'Custom RHEL Image Name Override (Optional)'
        required: false
        type: string
      windows_image_name_override:
        description: 'Custom Windows Image Name Override (Optional)'
        required: false
        type: string

permissions:
  contents: write

jobs:
  # ==========================================
  # STAGE 1: PREPARE (Calculate SemVer & Build Matrix)
  # ==========================================
  prepare:
    name: Prepare Pipeline & SemVer
    runs-on: ubuntu-latest
    outputs:
      new_version: ${{ steps.version.outputs.clean_version }}  
      matrix_config: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Calculate Next SemVer
        id: semver
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          dry_run: true
          default_bump: patch

      - name: Normalize Version String
        id: version
        run: |
          RAW="${{ steps.semver.outputs.new_version }}"
          CLEAN="${RAW#v}"          # strip leading 'v' if present
          CLEAN="${CLEAN%%-*}"      # strip pre-release suffix e.g. -SemanticVersioning.0
          echo "clean_version=$CLEAN" >> $GITHUB_OUTPUT

      - name: Set Dynamic Matrix
        id: set-matrix
        run: |
          CHOICE="${{ inputs.os_choice }}"
          if [[ -z "$CHOICE" || "$CHOICE" == "all" ]]; then
            echo 'matrix=["ubuntu", "rhel", "windows"]' >> $GITHUB_OUTPUT
          elif [[ "$CHOICE" == "ubuntu" ]]; then
            echo 'matrix=["ubuntu"]' >> $GITHUB_OUTPUT
          elif [[ "$CHOICE" == "rhel" ]]; then
            echo 'matrix=["rhel"]' >> $GITHUB_OUTPUT
          elif [[ "$CHOICE" == "windows" ]]; then
            echo 'matrix=["windows"]' >> $GITHUB_OUTPUT
          fi

  # ==========================================
  # STAGE 2: BUILD & SCAN (Parallel Matrix)
  # ==========================================
  matrix-factory:
    name: Build & Scan (${{ matrix.os }})
    needs: prepare
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false  # Allow the other OS leg to complete even if one fails
      matrix:
        os: ${{ fromJson(needs.prepare.outputs.matrix_config) }}

    env:
      AZURE_RG: ${{ vars.AZURE_RG }}
      AZURE_GALLERY: ${{ vars.AZURE_GALLERY }}
      AZURE_LOCATION: ${{ inputs.azure_region || vars.AZURE_LOCATION }}
      VM_SIZE: ${{ vars.VM_SIZE }}
      PKR_VAR_resource_group: ${{ vars.AZURE_RG }}
      PKR_VAR_gallery_name: ${{ vars.AZURE_GALLERY }}
      PKR_VAR_location: ${{ inputs.azure_region || vars.AZURE_LOCATION }}
      PKR_VAR_vm_size: ${{ vars.VM_SIZE }}
      PKR_VAR_subscription_id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      PKR_VAR_client_id: ${{ secrets.AZURE_CLIENT_ID }}
      PKR_VAR_client_secret: ${{ secrets.AZURE_CLIENT_SECRET }}
      PKR_VAR_local_admin_username: sysadmin
      PKR_VAR_local_admin_password: ${{ secrets.WINDOWS_LOCAL_ADMIN_PASSWORD }}
      PKR_VAR_ssh_password: ${{ secrets.WINDOWS_WINRM_PASSWORD }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Authenticate to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Setup HashiCorp Packer
        if: ${{ inputs.scan_only != true }}
        uses: hashicorp/setup-packer@main
        with:
          version: 'latest'

      # --- PACKER BUILD PHASE ---
      - name: Packer Init
        if: ${{ inputs.scan_only != true }}
        run: packer init .
        working-directory: ${{ matrix.os == 'ubuntu' && './packer' || matrix.os == 'rhel' && './packer-rhel' || './packer-windows' }}

      - name: Build Golden Image
        if: ${{ inputs.scan_only != true }}
        run: packer build -force -var "image_version=${{ needs.prepare.outputs.new_version }}" .
        working-directory: ${{ matrix.os == 'ubuntu' && './packer' || matrix.os == 'rhel' && './packer-rhel' || './packer-windows' }}

      # --- DEPLOY EPHEMERAL VM ---
      - name: Generate Ephemeral SSH Key
        run: ssh-keygen -t ed25519 -f ~/.ssh/scanner_key -N ""

      - name: Resolve Target Image Name
        id: image
        run: |
          if [[ "${{ matrix.os }}" == "ubuntu" ]]; then
            echo "name=${{ inputs.ubuntu_image_name_override || 'ubuntu-nvme-cis' }}" >> $GITHUB_OUTPUT
          elif [[ "${{ matrix.os }}" == "rhel" ]]; then
            echo "name=${{ inputs.rhel_image_name_override || 'rhel-nvme-cis' }}" >> $GITHUB_OUTPUT
          else
            echo "name=${{ inputs.windows_image_name_override || 'windows-server-2022-cis' }}" >> $GITHUB_OUTPUT
          fi

      - name: Deploy Ephemeral VM
        run: |
          IMAGE_ID=$(az sig image-definition show \
            --resource-group ${{ env.AZURE_RG }} \
            --gallery-name ${{ env.AZURE_GALLERY }} \
            --gallery-image-definition ${{ steps.image.outputs.name }} \
            --query id --output tsv)

          if [[ "${{ matrix.os }}" == "windows" ]]; then
            az vm create \
              --resource-group ${{ env.AZURE_RG }} \
              --name scan-${{ matrix.os }}-vm \
              --image $IMAGE_ID \
              --admin-username sysadmin \
              --ssh-key-values ~/.ssh/scanner_key.pub \
              --location ${{ env.AZURE_LOCATION }} \
              --size ${{ env.VM_SIZE }} \
              --nic-delete-option delete \
              --os-disk-delete-option delete \
              --nsg-rule SSH
          else
            az vm create \
              --resource-group ${{ env.AZURE_RG }} \
              --name scan-${{ matrix.os }}-vm \
              --image $IMAGE_ID \
              --admin-username sysadmin \
              --ssh-key-values ~/.ssh/scanner_key.pub \
              --location ${{ env.AZURE_LOCATION }} \
              --size ${{ env.VM_SIZE }} \
              --nic-delete-option delete \
              --os-disk-delete-option delete
          fi

          VM_IP=$(az vm show -d -g ${{ env.AZURE_RG }} -n scan-${{ matrix.os }}-vm --query publicIps -o tsv)
          echo "VM_IP=$VM_IP" >> $GITHUB_ENV
          sleep 20

          if [[ "${{ matrix.os }}" == "windows" ]]; then
            echo "Waiting for WinRM/SSH on Windows VM ($VM_IP)..."
            for i in {1..30}; do
              if nc -zw3 $VM_IP 22 2>/dev/null; then
                echo "Port 22 open after attempt $i."
                break
              fi
              echo "Attempt $i/30 — not ready, retrying in 15s..."
              sleep 15
              if [ $i -eq 30 ]; then
                echo "ERROR: Windows VM never became reachable."
                exit 1
              fi
            done
            sleep 30
          else
            echo "Waiting for SSH on ${{ matrix.os }} VM ($VM_IP)..."
            for i in {1..20}; do
              if ssh -o StrictHostKeyChecking=no -o ConnectTimeout=5 -i ~/.ssh/scanner_key sysadmin@$VM_IP exit 0 2>/dev/null; then
                echo "SSH is up after attempt $i."
                break
              fi
              echo "Attempt $i/20 — SSH not ready yet, retrying in 10s..."
              sleep 10
              if [ $i -eq 20 ]; then
                echo "ERROR: SSH never became available after 200s."
                exit 1
              fi
            done
          fi

      # --- SCAN PHASE ---
      - name: Run Ubuntu OpenSCAP & Download Report
        if: ${{ matrix.os == 'ubuntu' }}
        run: |
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/scanner_key sysadmin@${{ env.VM_IP }} << 'EOF'
            sudo apt-get update
            sudo apt-get install -y libopenscap8 unzip wget
            wget https://github.com/ComplianceAsCode/content/releases/download/v0.1.74/scap-security-guide-0.1.74.zip
            unzip scap-security-guide-0.1.74.zip
            sudo oscap xccdf eval \
              --profile xccdf_org.ssgproject.content_profile_cis_level1_server \
              --report /tmp/ubuntu-cis-report.html \
              scap-security-guide-0.1.74/ssg-ubuntu2204-ds.xml || true
            sudo mv /tmp/ubuntu-cis-report.html /home/sysadmin/ubuntu-cis-report.html
            sudo chown sysadmin:sysadmin /home/sysadmin/ubuntu-cis-report.html
          EOF
          scp -o StrictHostKeyChecking=no -i ~/.ssh/scanner_key \
            sysadmin@${{ env.VM_IP }}:/home/sysadmin/ubuntu-cis-report.html \
            ./ubuntu-cis-report.html

      - name: Run RHEL OpenSCAP & Download Report
        if: ${{ matrix.os == 'rhel' }}
        run: |
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/scanner_key sysadmin@${{ env.VM_IP }} << 'EOF'
            sudo dnf install -y openscap-scanner scap-security-guide
            sudo oscap xccdf eval \
              --profile xccdf_org.ssgproject.content_profile_cis_server_l1 \
              --report /tmp/rhel-cis-report.html \
              /usr/share/xml/scap/ssg/content/ssg-rhel9-ds.xml || true
            sudo mv /tmp/rhel-cis-report.html /home/sysadmin/rhel-cis-report.html
            sudo chown sysadmin:sysadmin /home/sysadmin/rhel-cis-report.html
          EOF
          scp -o StrictHostKeyChecking=no -i ~/.ssh/scanner_key \
            sysadmin@${{ env.VM_IP }}:/home/sysadmin/rhel-cis-report.html \
            ./rhel-cis-report.html

      - name: Prepare Windows Scan Script
        if: ${{ matrix.os == 'windows' }}
        run: |
          cat > /tmp/windows-scan.ps1 << 'PSEOF'
          Set-ExecutionPolicy Bypass -Scope Process -Force
          [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
          iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
          choco install openscap -y --no-progress
          Invoke-WebRequest -Uri 'https://github.com/ComplianceAsCode/content/releases/download/v0.1.74/scap-security-guide-0.1.74.zip' -OutFile 'C:\ssg.zip'
          Expand-Archive -Path 'C:\ssg.zip' -DestinationPath 'C:\ssg' -Force
          & 'C:\Program Files\OpenSCAP\oscap.exe' xccdf eval `
            --profile xccdf_org.ssgproject.content_profile_cis_level1_memberserver `
            --report 'C:\windows-cis-report.html' `
            'C:\ssg\scap-security-guide-0.1.74\ssg-windows_server_2022-ds.xml'
          PSEOF
        
      - name: Run Windows OpenSCAP & Download Report
        if: ${{ matrix.os == 'windows' }}
        run: |
          # Copy the script to the Windows VM
          scp -o StrictHostKeyChecking=no -i ~/.ssh/scanner_key \
            /tmp/windows-scan.ps1 \
            sysadmin@${{ env.VM_IP }}:"C:/windows-scan.ps1"

          # Execute it remotely
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/scanner_key \
            sysadmin@${{ env.VM_IP }} \
            "powershell -ExecutionPolicy Bypass -File C:\\windows-scan.ps1"
            
          scp -o StrictHostKeyChecking=no -i ~/.ssh/scanner_key \
            sysadmin@${{ env.VM_IP }}:"C:/windows-cis-report.html" \
            ./windows-cis-report.html
          
      # --- TEARDOWN ---
      - name: Destroy Ephemeral VM
        if: ${{ always() && env.VM_IP != '' }}
        run: |
          az vm delete --resource-group ${{ env.AZURE_RG }} --name scan-${{ matrix.os }}-vm --yes
          az network public-ip list \
            --resource-group ${{ env.AZURE_RG }} \
            --query "[?ipConfiguration==null].[name]" -o tsv \
            | xargs -I {} az network public-ip delete \
              --resource-group ${{ env.AZURE_RG }} --name {} || true

      - name: Upload Report Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-report
          path: ./*-cis-report.html

  # ==========================================
  # STAGE 3: PUBLISH (Tag & Release)
  # ==========================================
  release:
    name: Publish DevSecOps Release
    needs: [prepare, matrix-factory]
    runs-on: ubuntu-latest
    # Run unless the whole pipeline was cancelled.
    # fail-fast: false means matrix-factory 'completes' even with leg failures,
    # so we guard here to avoid tagging a broken build.
    if: ${{ !cancelled() && !failure() }}
    steps:
      - name: Download All Reports
        uses: actions/download-artifact@v4
        with:
          path: ./reports
          merge-multiple: true

      # --- VERSIONED RELEASE (normal build run) ---
      - name: Push Official Git Tag
        uses: mathieudutour/github-tag-action@v6.2
        if: ${{ inputs.scan_only != true }}
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          custom_tag: v${{ needs.prepare.outputs.new_version }}  # v-prefix matches release tag below

      - name: Publish Golden Image Release
        uses: softprops/action-gh-release@v2
        if: ${{ inputs.scan_only != true }}
        with:
          tag_name: v${{ needs.prepare.outputs.new_version }}
          name: "Golden Image Release v${{ needs.prepare.outputs.new_version }}"
          body: |
            Automated OpenSCAP CIS Level 1 Compliance Reports.
            **Azure Image Version:** `${{ needs.prepare.outputs.new_version }}`
          fail_on_unmatched_files: false
          files: ./reports/*-cis-report.html

      # --- AD-HOC SCAN RELEASE (scan_only run) ---
      - name: Publish Ad-Hoc Scan Report
        uses: softprops/action-gh-release@v2
        if: ${{ inputs.scan_only == true }}
        with:
          tag_name: scan-report-${{ github.run_number }}
          name: "Ad-Hoc Compliance Scan #${{ github.run_number }}"
          body: |
            Manual OpenSCAP CIS Level 1 evaluation against existing Gallery Images.
            *(Note: No new image versions were baked during this run).*
          fail_on_unmatched_files: false
          files: ./reports/*-cis-report.html
